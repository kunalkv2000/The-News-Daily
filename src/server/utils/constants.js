// utils/constants.js

// API URL constants (for news API or any other service you use)
const API_URL = "https://newsapi.org/v2/";
const NEWS_API_KEY = "";

// Frontend URLs for various pages (useful for redirects or email links)
const FRONTEND_URL = "http://localhost:5173";

// Email templates or other predefined strings
const PASSWORD_RESET_SUBJECT = "Password Reset Request";

export default { API_URL, NEWS_API_KEY, FRONTEND_URL, PASSWORD_RESET_SUBJECT };
